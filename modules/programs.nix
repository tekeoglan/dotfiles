# ~/.config/home-manager/modules/programs.nix
{ ... }:

{
  # programs.neovim = {
  #   enable = true;
    # If you use Neovim plugins that are available in Nixpkgs, you can add them here.
    # For example, if you use 'vim-sensible'
    # plugins = with pkgs.vimPlugins; [
    #   vim-sensible
    #   # Add more plugins as needed. Use search.nixos.org to find them!
    # ];

    # We'll manage the actual config files with home.file, so we don't use
    # extraConfig or initLua here.
  # };

  # ... other program configurations
}
